server:
  port: 8000
spring:
  application:
    name: exchange-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/exchange_services
    username: root
    password: '@Biel568204'
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
    show_sql: false
    open-in-view: false
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
